= render 'accounts/dashboard/nav'
.alert.alert-info
  | Send messages to the account email address, and they will be stored here.
table.table-sm
  tr
    th Account Email Address
  tr
    td
      pre account+#{@account.code}@portal.zdn.enterprises
table.table.table-sm.table-bordered.table-striped.table-hover
  thead
    tr
      th Sender
      th Subject
      th[colspan="2"] Actions
  tbody
    - @emails.each do |email|
      tr
        td = email.data['From']
        td = email.data['Subject']
        td = link_to 'JSON', account_inbound_email_path(@account.code, email.token, format: :json), target: '_blank'
        td = link_to 'HTML', account_inbound_email_path(@account.code, email.token, format: :html), target: '_blank'
        td = link_to 'TXT', account_inbound_email_path(@account.code, email.token, format: :text), target: '_blank'